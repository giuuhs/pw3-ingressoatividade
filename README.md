# Dupla

# Giulia e Nickolas

create table TBL_SESSAO(
    ID_SESSAO BIGINT PRIMARY KEY IDENTITY,
    DATA DATE NOT NULL,
    HORARIO DATETIME DEFAULT GETDATE(),
    FILME_ID BIGINT,
    FOREIGN KEY (FILME_ID) REFERENCES TBL_FILME(ID_FILME),
    PRECO FLOAT,
    SALA VARCHAR(20)
)